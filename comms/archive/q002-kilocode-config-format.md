# Question: .kilocode Directory Configuration Format

**Section:** PRD §4.2, §9
**Status:** OPEN
**Date:** 2026-02-13

## Issue
The PRD mentions mounting `.kilocode` directory for API keys but doesn't specify:
- What format should the directory's contents use?
- What API keys are expected?
- Is there a specific file structure or naming convention?
- What should happen if the directory is missing or empty?

## Context
We need to implement validation for the `.kilocode` directory during `switchboard build` per BACKLOG.md, but the expected format is undefined.

## Question
Please clarify the expected structure, file format, and validation rules for the `.kilocode` configuration directory.

## Proposed Resolution (to be filled)
